# Supabase の publishable キー。クライアントに露出する前提のキーなので公開して問題ない。
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxx

# 観客用 Web のデプロイ先。参加コードの URL と QR コードがこれを使う。
# 未設定なら http://localhost:3000 に落ちるが、その URL は観客のスマホからは開けない
# ＝ QR が意味を持たないので、デプロイしたら必ず設定する。
VITE_AUDIENCE_BASE_URL=https://<deployed-host>

# 直接配布版のStripe Checkoutと、MAS版のStoreKit検証APIは観客WebのRoute Handlerにある。
# 未設定なら VITE_AUDIENCE_BASE_URL に落ちる。**どちらも空だと法務ページ・サポート・
# 領収書のリンクが `Audience URL is not configured` で全部死ぬ**（5.1.1(i) に直撃する）。
VITE_BILLING_API_BASE_URL=https://<deployed-host>

# MAS版のIAP商品ID。サーバのAPPLE_EVENT_PASS_PRODUCT_IDと**必ず一致させる**
# （ずれると /attempt は通るのに fulfill が product mismatch で落ちる）。
# 商品タイプは **Non-Renewing Subscription**。7日間の期間限定アクセスを Consumable で
# 売ると Purchasability Type で差し戻される。App Store Connect は既存 IAP のタイプ変更も
# 商品IDの再利用も許さないので、Consumable で作ってしまった場合は新しいIDを採ること。
VITE_APPLE_EVENT_PASS_PRODUCT_ID=app.layertalk.presenter.event_pass

# 直接配布版だけが使う、署名済みEvent PassリースのEd25519 SPKI PEM。
# `npm run build:mas` では検証コードごとバンドルから除外される。
VITE_ENTITLEMENT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----
